import Calendar from '@/common/display/Calendar';
import { Box } from '@mui/material';

const Orders = () => {
  return (
    <Box marginTop={4}>
      <Calendar />
    </Box>
  );
};

export default Orders;
